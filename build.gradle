version = 1.0

task dist(type: Zip) {
    destinationDir = file('dist')
    baseName = 'pipelines'

    from('rdkit') {
        includes = ['**.py','**.nf','**.ded']
        into 'pipelines/rdkit'
    }
}

task clean(type: Delete) {
    delete 'dist'
}